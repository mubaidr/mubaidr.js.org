export default defineAppConfig({
  ui: {
    colors: {
      primary: "amber",
      secondary: "blue",
      neutral: "zinc",
    },
    badge: {
      defaultVariants: {
        variant: "subtle",
      },
    },
    button: {
      defaultVariants: {
        color: "primary",
      },
    },
  },
})
